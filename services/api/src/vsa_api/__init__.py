"""Vertical AI Agent Platform — API monolith.

Placeholder package. The platform layer and bounded-context modules land in
later deliverables (D3 onward). Module import boundaries are declared in the
repository-root ``.importlinter``.
"""
